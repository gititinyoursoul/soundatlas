Create event-framing Markdown from the route concept and event list. For each active Event, propose a product-facing Event title (usually under 90 characters) and one or more ordered story sections with distinct, story-serving headings and source-grounded prose. Treat the headings as editorial proposals for Human review: preserve a supplied editorial heading exactly when reusing it, and do not normalize it into `summary`, `significance`, `What happened`, or `Why it matters`. Include place decisions, connection rationale, source needs, and wording risks. Do not return seed JSON or claim publication readiness.
