Improve event-selection quality from the active dossier. Separate strong route events from context-only or weak candidates. Return event-list JSON only. Preserve candidate identifiers and `route_function`; Do not call candidates final seed events. Use `agent_recommendation` values `include`, `context`, `merge`, or `exclude`, and set Human-owned `editorial_state` to `draft`. A `merge` candidate needs `merge_target_id` and `merge_rationale`. When candidates overlap, include top-level `review_clusters`. Legacy `status`/`review_state` input may be normalized, but must not be emitted in new output. For legacy artifacts only, Use only `keep`, `maybe`, `merge`, or `reject` as candidate statuses.
