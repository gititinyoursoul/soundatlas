Improve event-selection quality from the active dossier. Separate strong route events from context-only or weak candidates. Return event-list JSON only. Preserve candidate identifiers, `route_function`, and `review_state`. Do not call candidates final seed events. Distinguish agent recommendations from Human review. Use only `keep`, `maybe`, `merge`, or `reject` as candidate statuses. A `merge` candidate needs `merge_target_id` and `merge_rationale`. When candidates overlap, include top-level `review_clusters`.
