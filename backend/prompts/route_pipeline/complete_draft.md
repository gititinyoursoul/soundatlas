Create one generated route result from the candidate outline and selected dossier. The candidate outline is agent planning input, not a Human-approved roster: you may add, omit, merge, split, or reorder candidates, but must account for every outline candidate once. Never invent source support, approve content, publish seed data, or claim publication readiness.

The `_meta` object must include `contract_version: "3"`, the exact route ID, `source_outline: "candidate-outline.json"`, `content_review_status: "draft"`, and the supplied source basis. The canonical place catalog below is authoritative for `reuse` and `update` decisions. Geodata research is advisory input recorded in this artifact; never claim Human approval or depend on a live lookup during preview or publication.

For every Candidate provide `candidate_id`, `agent_recommendation`, `editorial_state`, `route_entry_role`, `years`, `place`, `working_title`, `route_function`, `decision_rationale`, `review_question`, `source_leads`, `risk_notes`, `next_action`, and `composition`. `agent_recommendation` is one of `include`, `context`, `merge`, `exclude`; `editorial_state` is exactly `draft`; `route_entry_role` is one of `active`, `context`, or `exclude`. A `context` route entry remains a full map/timeline event, uses an honest existing place representation, and must include `next_evidence_task` with non-empty `missing_evidence`, `target_claim`, `target_place`, and `expected_output` strings. An `exclude` entry remains in candidate accounting but has no reader-facing event requirement. `composition.outcome` is one of `active`, `omitted`, `merged_into`, `split_into`, `added`; `composition` also has non-empty `reason` and string-array `related_candidate_ids`. Other considered candidates require `preview.summary` and `preview.significance`.

`route_concept` is a non-empty Markdown string. `sequence` must exactly equal the Candidate IDs whose `composition.outcome` is `active` or `added`, in the same order in which those Candidates appear in the `candidates` array. Every Event is a complete current-schema framing draft: its ID matches one active/added Candidate ID and it includes `id`, `route_id`, `place_id`, `place_ids`, `default_place_id`, `place_relationships`, `title`, `year_start`, `year_end`, `story_sections`, `tags`, `content_review_status`, `source_urls`, `media_links`, and `image_links`. Do not include `summary` or `significance` on a section-based Event. `story_sections` is a non-empty ordered array; every item contains exactly one non-empty `heading` and one non-empty `body`. Headings are editorial proposals: preserve supplied editorial heading wording and order exactly when reusing it, never replace headings with generic global labels, and never claim that generated wording is Human-approved. Media item `type` is one of `track`, `album`, `playlist`, `video`, `search`, and media `content_review_status` is `draft` or `reviewed`.

Each `place_relationships` item must use exactly `from_place_id`, `to_place_id`, `directionality`, `context_label`, and `source_urls`. `directionality` is `undirected`, `forward`, or `reciprocal`; endpoints must be distinct; `source_urls` must contain at least one real URL. If no source-backed relationship between two distinct places exists, emit `place_relationships: []` rather than inventing a relationship or URL.

Every Place is a route-level framing decision: `decision` is `reuse`, `new`, or `update`, with `place_id` and `source_place_text`. Emit exactly one coherent Place decision for each `place_id` referenced by an Event. Events may share and revisit that Place through their ordered `place_ids`; do not duplicate a Place decision to express event-specific narrative context. The first referenced place is not automatically the default: choose `default_place_id` deliberately from the Event's `place_ids`.

A `reuse` decision may use only an ID from the supplied canonical place catalog, carries no replacement Place or spatial update, and preserves the canonical record unchanged. A `new` decision must not collide with the catalog and contains a complete seed-shaped `place` record with reviewed non-placeholder latitude and longitude, optional Polygon or MultiPolygon geometry, required conditional geometry precision/provenance, and the normal Place fields. An `update` decision may use only a canonical ID and contains `spatial_update` with one or more of: `latitude`, `longitude`, `geometry`, `geometry_precision`, `geometry_source_type`, `geometry_source_url`, `geometry_source_note`, and `geometry_license`. Never place names, boroughs, types, summaries, content state, or historical sources in `spatial_update`. Do not emit `update` when spatial data is unchanged. Updates remain unapproved proposals until the Human explicitly approves the bound route-review place record.

Connections are deferred from the MVP: emit an empty `connections` array and do not propose new relationship records. Existing legacy Connection artifacts remain compatibility input only. Every phase-coverage entry has `phase`, `status` (`covered`, `narrowed`, or `gap`), and known Candidate IDs. Every Finding has a non-empty `message`, optional known `candidate_id`, and owner `candidate_composition`, `active_event`, `active_route`, `source_media`, or `technical`. `warnings` and `technical_errors` are string arrays.
